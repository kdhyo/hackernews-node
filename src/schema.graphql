type Query {
  links: [Link!]!
  link(id: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}

type Mutation {
  update(id: String!, url: String!, description: String!): Link!
  dete(id: String!): [Link]
}
